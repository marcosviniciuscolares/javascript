/*
for (let pos=0; pos < valores.length; pos++) {
    console.log(`The position ${pos} have ${valores[pos]} value.`)
}
*/

let valores = [8, 1, 7, 2, 1]

console.log(valores)

for (let pos in valores) {
    console.log(`The psoition ${pos} have ${valores[pos]} value.`)
}

